# Order Execution Service

This project implements a Order Execution Service for a crypto trading bot. It fetches real-time market data using REST APIs and WebSocket connections.

## Getting Started

To run this project, you will need Node.js and npm installed on your system.

### Installation

1. Clone the repository
2. Install dependencies: `npm install`
3. Build the project: `npm run build`
4. Start the service: `npm start`

## Configuration

The service requires certain environment variables to be set in a `.env` file:

- `API_KEY`: Your API key for the data source.
- `WS_URL`: WebSocket URL for real-time data.
- `REST_API_URL`: REST API URL for fetching market data.

## Usage

The service provides endpoints to fetch market data and establishes WebSocket connections to stream real-time data.
